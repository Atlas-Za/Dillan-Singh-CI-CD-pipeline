name: Node.js CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14' # Choose the desired Node.js version

      - name: Install dependencies
        run: npm install

      - name: Build
        run: .\SLD521FA2DillanSingh20232233.sln\SLD521_Formative_2

      - name: Run tests
        run: echo "Running tests..."

  deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14' # Choose the desired Node.js version

      - name: Install dependencies
        run: npm install

      - name: Deploy to staging (replace with actual deployment steps)
        run: echo "Deploying to staging..."